

1. In server virtualization, which component directly manages the physical hardware and exposes virtualized resources to VMs?
- Guest OS
- Hypervisor
- Open vSwitch
- Docker Engine

2. Docker containers are described as isolated processes that primarily share what with other containers?
- Hypervisor
- Host kernel
- Guest kernel
- Physical NIC firmware

3. In Docker networking, external network connectivity is commonly provided using:
- MPLS label switching
- NAT
- iBGP
- VLAN flooding

4. In SR-IOV, what is a "virtual function" on the NIC?
- A full physical Ethernet port
- A software bridge inside the hypervisor
- A VLAN tag applied by the controller
- A simple queue with transmit/receive functionality mapped to a VM

5. In Open vSwitch design, where do the "smarts" (rule/policy decisions) primarily reside?
- User space
- NIC hardware pipeline
- Physical switch firmware
- VM guest OS

6. In Open vSwitch, what is primarily handled in the kernel?
- Fast/common-case packet forwarding
- High-level policy computation
- Tenant topology design
- Directory-server lookups

7. Mininet is best described as:
- A WAN traffic engineering optimizer
- A hardware NIC virtualization standard
- A network emulator that can create realistic virtual networks on one machine
- A proprietary router configuration platform

8. In SDN, the "logically centralized controller" is primarily intended to:
- Replace all physical links with wireless
- Keep distributed switching devices simple while centralizing intelligence
- Eliminate the need for data planes
- Run only inside NIC firmware

9. VL2 uses "oblivious routing," meaning that a flow's path:
- Always follows the shortest path only
- Depends on current traffic matrix measurements
- Does not depend on the current traffic matrix
- Changes per-packet based on CPU load

10. Google's B4 is characterized as:
- A public Internet peering solution
- A private WAN connecting Google data centers globally using SDN ideas
- A hypervisor for VMs
- A Linux container runtime
